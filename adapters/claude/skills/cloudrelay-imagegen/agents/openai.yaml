interface:
  display_name: "CloudRelay ImageGen"
  short_description: "默认通过 CloudRelay 异步生成、编辑并保存图片"
  default_prompt: "使用 $cloudrelay-imagegen 根据我的提示词生成或编辑图片，并将结果保存到工作区。"

policy:
  allow_implicit_invocation: true
